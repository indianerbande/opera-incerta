{
  "name": "@opera-incerta/spike-editor-codemirror",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Disposable spike: does CodeMirror 6 satisfy the editor gate in testing.md \u00a72.8?",
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && esbuild src/renderer.ts --bundle --format=iife --outfile=dist/renderer.js && esbuild src/main.ts --bundle --platform=node --format=cjs --external:electron --outfile=dist/main.cjs",
    "spike": "electron dist/main.cjs"
  },
  "devDependencies": {
    "@codemirror/commands": "6.11.0",
    "@codemirror/state": "6.7.4",
    "@codemirror/view": "6.43.11",
    "@opera-incerta/core": "workspace:*",
    "@opera-incerta/workbench": "workspace:*",
    "electron": "44.4.2",
    "esbuild": "0.28.2"
  }
}
