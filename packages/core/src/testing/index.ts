/**
 * What the core offers for testing other packages: the editor adapter contract
 * suite. Not part of the main entry, so it never ships.
 */
export { runEditorAdapterContract } from './editor-adapter-contract.js';
export type { ContractCase } from './editor-adapter-contract.js';
